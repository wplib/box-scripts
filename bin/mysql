#!/bin/bash


source /opt/box/cli/includes/functions

# Creates IMAGES and STACK env variables.
readJSON

DB="${STACK[wordpress/dbserver]}"

# echo "box container shell ${DB} mysql $@"
box container shell ${DB} mysql "$@"

