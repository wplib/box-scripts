{
    "type": "container",
    "description": "The WordPress CLI command line tool",
    "network": "pressboxx",
    "restart": "no",
    "volumes": [
        { "host": "${PROJECT_BASE}/${CURRENT_PROJECT}/www", "container": "/var/www", "mount": true }
    ]
}
